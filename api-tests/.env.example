# Base URLs (localhost when running against docker-compose)
AUTH_BASE_URL=http://localhost:9000
AIRLINES_BASE_URL=http://localhost:9001
CRUISES_BASE_URL=http://localhost:9002

# Optional: reuse an existing user in global-setup (otherwise a unique user is registered each run)
# E2E_USER_EMAIL=tester@example.com
# E2E_USER_PASSWORD=SecurePass123!

# Playwright / CI
CI=
